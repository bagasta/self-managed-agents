version: "1.1.0"
triggers: ["edit", "update", "change-agent", "agent-failure", "agent-complaint", "runtime-diagnosis"]
supported_states: ["idle", "discovery", "awaiting_confirmation", "updating", "verifying"]
allowed_tool_groups: ["lifecycle", "compose", "create"]
trust_level: "system"
primary: true
enabled: true
